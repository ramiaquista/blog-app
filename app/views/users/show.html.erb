<h1>This shows the user data</h1>

<div>
   <h2>User name: </h2> $<%= @user.name%><br />
   <p>Post counter:</p> <%= @user.posts_counter %><br />
</div>
<div>
   <h4>Bio</h4>
   <p><%= user.bio %></p>
</div>
<% if @posts.blank? %>
<p>This user has no posts</p>
<% else %>
<ul>
   <% @posts.each do |post| %>
    <li><%= post.title%><br /><p><%= pos.text%></p></li>
   <% end %>
   <button>See all posts</button>
</ul>